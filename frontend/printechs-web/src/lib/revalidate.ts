/** Shared ISR/cache window for ERP-backed pages (seconds). */
export const REVALIDATE_SECONDS = 300;

/** Longer cache window for stable catalogue/list pages (seconds). */
export const REVALIDATE_STABLE_SECONDS = 3600;
